# 项目提示词记录

本文档记录了 libmarkdown 项目开发过程中用户输入的所有提示词。

---

## 提示词列表

### 1. 项目设计 (第1条)

```
让我们来设计一个用来读取分析 Markdown 格式文件的库，支持读取markdown格式文件，提供API解析文档结构，抽取内容。设计文档要描述清楚markdown的结构，并且给出必须的解析功能。这个项目用 纯 C 语言来实现。
```

### 2. 设计文档保存 (第2条)

```
设计文档以 .md 格式保存到 .doc目录里
```

### 3. 枚举接口需求 (第3条)

```
需要有个接口用来 枚举 markdown的每个元素，返回元素对象，元素对象包括 元素的类型，内容，属性
```

### 4. 迭代器API设计 (第4条)

```
md_iterator_next 可以直接返回当前的 md_element_t 并且把迭代器指向下一个节点，如果没有更多元素了，就返回null
```

### 5. 元素类型定义 (第5条)

```
这个设计不错，完成所有的 元素类型的定义
```

### 6. 保存设计 (第6条)

```
保存吧
```

### 7. 目录结构1 (第7条)

```
include
```

### 8. 目录结构2 (第8条)

```
include下没必要再有个md目录，所有头文件直接放到 include目录下就可以
```

### 9. 目录注释 (第9条)

```
给上面的目录结构写上注释
```

### 10. 主头文件命名 (第10条)

```
把 md.h 改成 markdown.h 用全称不容易跟别人冲突
```

### 11. 目录结构3 (第11条)

```
在 include 下面建立一个 markdown 子目录，把所有头文件放到里面
```

### 12. 开始实现 (第12条)

```
好了，可以开始实现了
```

### 13. 测试准备 (第13条)

```
编译结果输出到 ./bin 目录，创建一个 用来测试的 .md 文件，包含所有Markdown标准里的元素，编译 test 程序并检验运行结果
```

### 14. 测试覆盖率 (第14条)

```
为libmarkdown编写测试用例，代码行覆盖率要超过 80%
```

### 15. 代码行覆盖率 (第15条)

```
我需要的不仅仅是函数覆盖率，而是代码行覆盖率，要超过 80 %
```

### 16. 提高覆盖率 (第16条)

```
the coverage of parser and extractor is low than 80%, could you make them greater?
```

### 17. 分析未覆盖代码 (第17条)

```
parser.c 里面没有被测试覆盖的代码是哪些，为什么没有测试覆盖
```

### 18. GitHub推送 (第18条)

```
我怎么才能把当前项目的代码 推到我的github账号上
```

### 19. 设计文档 (第19条)

```
添加 design.md 到 doc 目录，描述整个项目的目标，架构，和相关设计，解释项目内容和使用方法
```

### 20. README完善 (第20条)

```
完善 readme 文档，简要介绍下项目，包括项目概况，下载编译测试入门方式，授权方式
```

### 21. 中文文档 (第21条)

```
readme 默认是英文的，再提供  readme.zhCN.md 支持中文，在 readme里 提供指向中文版的链接
```

### 22. 总结 (第22条)

```
总结一下，我在这个项目里输入的所有提示词
```

### 23. 提示词记录 (第23条)

```
列出我从创建项目以来输入的所有提示词原文
```

---

## 项目开发概览

- **总提示词数**: 23条
- **开发周期**: 从项目设计到文档完善
- **主要功能**: 纯C语言Markdown解析库

### 开发阶段

1. **设计阶段**: 1-12条 - 确定API设计和目录结构
2. **实现阶段**: 13条 - 开始编码实现
3. **测试阶段**: 14-17条 - 编写测试提高覆盖率
4. **部署阶段**: 18条 - 推送到GitHub
5. **文档阶段**: 19-21条 - 完善项目文档
6. **总结阶段**: 22-23条 - 项目总结
